# 全通劳务 AI 招工助手：阿里云受控部署指南

> 本文是云端部署模板，不表示云端已经部署或通过生产验收。执行前必须完成 `项目工程化/07-发布上线/发布前检查清单.md`，并取得发布负责人的授权。

## 1. 安全目标

公网访问只允许经过 TLS 的 Nginx 反向代理；Streamlit 和 Ollama 只监听本机回环地址。首版演示应限制到明确的测试网段或额外访问控制，不能把应用端口直接暴露给公网。

```text
浏览器 -> HTTPS :443 (Nginx + TLS + 访问控制)
       -> 127.0.0.1:8501 (Streamlit)
       -> 127.0.0.1:11434 (Ollama)
管理端 -> SSH :22（仅管理 CIDR、密钥登录、非 root 用户）
```

禁止对公网开放 `8501` 或 `11434`。不要使用 IP 直连 HTTP、`--server.address 0.0.0.0`、root 密码 SSH，也不要把 `.env`、数据库、聊天记录、Chroma 索引或模型缓存提交到仓库。

## 2. 上线前准备

- 经过审核的 Git 标签或构建产物，而不是服务器上的临时代码。
- 已解析的域名和有效 TLS 证书；临时验证也应使用受控测试域名。
- 由部署负责人生成的随机长 `DEMO_PASSWORD`，通过受限环境文件或密钥管理器注入。
- 经过审核的岗位/FAQ 数据、备份、校验值和回滚版本。
- 明确的管理网段，例如 `<ADMIN_CIDR>`；SSH 禁止对 `0.0.0.0/0` 开放。

## 3. 创建 ECS 与安全组

建议从 Ubuntu 22.04 LTS、至少 2 核 4 GB 内存、40 GB 磁盘起步；实际模型延迟和并发必须在目标环境压测后确认。

安全组入方向只配置以下端口：

| 协议/端口 | 来源 | 用途 |
|---|---|---|
| TCP 22 | `<ADMIN_CIDR>` | SSH 管理，仅密钥登录 |
| TCP 80 | `0.0.0.0/0`、`::/0` | HTTPS 跳转或 ACME 验证 |
| TCP 443 | 首版推荐 `<TEST_CIDR>`；扩大前需安全评审 | Nginx HTTPS 入口 |

不要创建 TCP 8501 或 TCP 11434 的入方向规则。安全组变更需记录负责人、时间、原因和回滚方式。

## 4. SSH 与运行账号

使用云控制台创建或导入 SSH 密钥。以云镜像提供的初始管理员身份登录后，先创建普通管理用户并验证密钥登录；保持当前控制台会话，确认新会话可用后才禁用 root 和密码登录。

```bash
# <ADMIN_USER> 为人工管理账号，不使用 root 作为日常登录身份。
sudo adduser --disabled-password --gecos "" <ADMIN_USER>
sudo usermod -aG sudo <ADMIN_USER>
sudo install -d -o <ADMIN_USER> -g <ADMIN_USER> -m 0700 /home/<ADMIN_USER>/.ssh
sudoedit /home/<ADMIN_USER>/.ssh/authorized_keys
sudo chown <ADMIN_USER>:<ADMIN_USER> /home/<ADMIN_USER>/.ssh/authorized_keys
sudo chmod 0600 /home/<ADMIN_USER>/.ssh/authorized_keys

# 在另一终端测试成功后再关闭密码登录。
ssh -i ~/.ssh/<KEY_FILE> <ADMIN_USER>@<SERVER_HOST>

sudo tee /etc/ssh/sshd_config.d/99-labor-ai.conf >/dev/null <<'EOF'
PermitRootLogin no
PasswordAuthentication no
KbdInteractiveAuthentication no
PubkeyAuthentication yes
EOF
sudo sshd -t
sudo systemctl reload ssh
```

## 5. 安装运行依赖

以下命令以已验证的普通管理用户执行。Ollama 的安装方式和版本必须经过组织批准并记录在发布记录中，不要在生产终端直接粘贴未经审阅的远程脚本。

```bash
sudo apt update
sudo apt install -y python3 python3-venv python3-pip git nginx curl

sudo adduser --system --group --home /opt/labor-ai-demo laborai
sudo install -d -o laborai -g laborai -m 0750 /opt/labor-ai-demo /opt/backups/labor-ai-demo

# 按已批准的 Ollama 安装流程完成安装后：
sudo systemctl enable --now ollama
ollama pull qwen2.5:3b
curl --fail --max-time 5 http://127.0.0.1:11434/api/tags
sudo ss -lntp | grep 11434
```

确认 Ollama 只监听本机；若监听到非 loopback 地址，先修正服务配置并复查端口。

## 6. 部署经过验收的版本

服务器不应成为开发工作区，也不应上传本地虚拟环境、日志、数据库、索引或 `.env`。

```bash
sudo -u laborai git clone <REPOSITORY_URL> /opt/labor-ai-demo
cd /opt/labor-ai-demo
sudo -u laborai git fetch --tags
sudo -u laborai git switch --detach <RELEASE_TAG>
sudo -u laborai python3 -m venv venv
sudo -u laborai venv/bin/python -m pip install --upgrade pip
sudo -u laborai venv/bin/python -m pip install -r requirements.txt
```

创建仅 root 和服务账号可读的环境文件：

```bash
sudo install -o root -g laborai -m 0640 /dev/null /etc/labor-ai-demo.env
sudoedit /etc/labor-ai-demo.env
```

```ini
DEMO_PASSWORD=<由部署负责人生成的随机长密码>
OLLAMA_MODEL=qwen2.5:3b
OLLAMA_BASE_URL=http://127.0.0.1:11434
NO_PROXY=localhost,127.0.0.1
```

确认脱敏数据已按受限流程放入 `data/jobs.json` 和 `data/faq.md` 后，构建索引并记录结果：

```bash
cd /opt/labor-ai-demo
sudo -u laborai venv/bin/python check_environment.py --build-index
```

## 7. 创建 systemd 服务

创建 `/etc/systemd/system/labor-ai-demo.service`：

```ini
[Unit]
Description=Labor AI Demo Streamlit service
After=network-online.target ollama.service
Wants=network-online.target

[Service]
Type=simple
User=laborai
Group=laborai
WorkingDirectory=/opt/labor-ai-demo
EnvironmentFile=/etc/labor-ai-demo.env
ExecStart=/opt/labor-ai-demo/venv/bin/streamlit run app.py --server.address 127.0.0.1 --server.port 8501 --server.headless true
Restart=on-failure
RestartSec=5
TimeoutStopSec=30
NoNewPrivileges=true
PrivateTmp=true

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now labor-ai-demo
sudo systemctl status labor-ai-demo --no-pager
curl --fail --max-time 5 http://127.0.0.1:8501/_stcore/health
sudo ss -lntp | grep 8501
```

输出中 `8501` 必须为 `127.0.0.1:8501` 或 `::1:8501`，不能是 `0.0.0.0:8501`。

## 8. 配置 Nginx、TLS 与访问控制

先为 `<DOMAIN>` 获取并验证 TLS 证书，再启用公网入口。配置包含 HTTP 到 HTTPS 跳转、CIDR 白名单和 Streamlit WebSocket 代理头；扩大访问范围前必须完成访问控制、限流、日志脱敏和安全评审。

创建 `/etc/nginx/sites-available/labor-ai-demo`：

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name <DOMAIN>;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name <DOMAIN>;

    ssl_certificate     /etc/letsencrypt/live/<DOMAIN>/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/<DOMAIN>/privkey.pem;

    allow <TEST_CIDR>;
    deny all;

    location / {
        proxy_pass http://127.0.0.1:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_read_timeout 300;
        proxy_buffering off;
    }
}
```

启用前确认域名、证书路径和 `<TEST_CIDR>` 均已替换；不要把占位符原样部署。

```bash
sudo ln -s /etc/nginx/sites-available/labor-ai-demo /etc/nginx/sites-enabled/labor-ai-demo
sudo nginx -t
sudo systemctl reload nginx
curl --fail --max-time 10 https://<DOMAIN>/_stcore/health
```

## 9. 验收、更新与回滚

1. 从批准的测试网段通过 `https://<DOMAIN>` 登录，确认浏览器无证书或混合内容警告。
2. 完成岗位检索、FAQ、来源展示、多轮追问和异常输入冒烟，不要只检查网页是否打开。
3. 观察 Nginx、systemd、Ollama 的错误日志、CPU、内存、磁盘和模型状态。
4. 更新前备份 `data/`、SQLite 数据库和当前 Git SHA；更新后先验证再开放流量。
5. 认证、数据、索引或模型异常时，限制 Nginx 入口并按 `项目工程化/07-发布上线/回滚方案.md` 回滚，不删除证据或唯一数据源。

```bash
sudo systemctl status labor-ai-demo ollama nginx --no-pager
sudo journalctl -u labor-ai-demo -n 100 --no-pager
curl --fail --max-time 5 http://127.0.0.1:11434/api/tags
curl --fail --max-time 5 http://127.0.0.1:8501/_stcore/health
df -h /opt/labor-ai-demo /opt/backups
```

更多发布、备份、事故响应和复盘步骤见 `项目工程化/07-发布上线`、`08-部署运维` 和 `09-复盘运营`。真实公网部署须完成这些目录定义的门禁，才可称为上线。

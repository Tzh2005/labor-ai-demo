# 劳务派遣AI客服 - 阿里云服务器配置指南

## 一、配置推荐（分阶段）

### 🎯 阶段1: Demo演示期（推荐配置）

**场景**：中秋节演示 + 内部测试（1-2个月）

#### 最低配置（够用）✅ 推荐你现在用这个
```
CPU: 2核
内存: 4GB
硬盘: 40GB
带宽: 1Mbps
系统: Ubuntu 22.04 LTS

价格: 试用免费 或 ¥50-80/月
并发: 支持5-10人同时使用
响应: 3-5秒（用小模型可以到2-3秒）
```

#### 推荐配置（更好，但不必要）
```
CPU: 4核
内存: 8GB
硬盘: 60GB
带宽: 3Mbps
系统: Ubuntu 22.04 LTS

价格: 约¥150-200/月
并发: 支持20-30人同时使用
```

---

### 🚀 阶段2: 正式运营期（3个月后）

**场景**：真实运营，服务公司客服和工人

#### 生产配置
```
CPU: 8核
内存: 16GB
硬盘: 100GB SSD
带宽: 5Mbps
系统: Ubuntu 22.04 LTS

价格: 约¥300-400/月
并发: 支持100+人同时使用
```

---

## 二、💡 你现在应该选：2核4GB

### 为什么选这个？

1. ✅ **试用免费** - 阿里云试用实例正好是这个配置
2. ✅ **Demo够用** - 演示阶段只有几个人测试
3. ✅ **省钱** - 即使付费也只要¥50-80/月
4. ✅ **可升级** - 后续可以随时扩容

### 能做什么？

✅ 运行Ollama + qwen2.5:3b模型（推荐）  
✅ 运行Streamlit前端  
✅ 支持5-10人同时使用  
✅ 响应速度2-3秒（可接受）  
✅ 中秋节演示完全够用  

### 不能做什么？

❌ 跑大模型（7b会卡顿，建议用3b）  
❌ 支持100+并发  
❌ 响应速度不如本地（没GPU）  

---

## 三、阿里云ECS快速配置步骤

### Step 1: 创建实例

1. 登录阿里云控制台
2. 点击「云服务器ECS」→「实例」→「创建实例」

**选择配置**：
```
地域：深圳（离公司近，延迟低）
实例规格：通用型 g6 或 计算型 c6
配置：2核4GB
镜像：Ubuntu 22.04 64位
系统盘：40GB 高效云盘
网络：按使用流量，带宽1-3Mbps
```

### Step 2: 配置安全组

**开放端口**：
```
入方向规则：
- 22/TCP   (SSH登录)
- 80/TCP   (HTTP访问)  
- 8501/TCP (Streamlit服务)

来源：0.0.0.0/0 (允许所有IP访问)
```

### Step 3: 获取公网IP

创建完成后，在实例列表找到你的公网IP，例如：`47.xxx.xxx.xxx`

---

## 四、一键部署脚本（复制粘贴运行）

### SSH登录到服务器

```bash
# 在本地电脑运行（替换成你的IP）
ssh root@你的服务器IP

# 输入密码（控制台设置的root密码）
```

### 运行部署脚本

登录后，复制粘贴以下命令：

```bash
# ===== 劳务派遣AI客服 - 一键部署 =====

# 1. 更新系统
echo "📦 更新系统..."
sudo apt update && sudo apt upgrade -y

# 2. 安装Ollama
echo "🤖 安装Ollama..."
curl -fsSL https://ollama.com/install.sh | sh

# 3. 启动Ollama
echo "🚀 启动Ollama..."
sudo systemctl start ollama
sudo systemctl enable ollama

# 4. 下载小模型（3B，2核4GB能跑）
echo "📥 下载AI模型（需要5-10分钟）..."
ollama pull qwen2.5:3b

# 5. 安装Python
echo "🐍 安装Python..."
sudo apt install -y python3 python3-pip python3-venv git

# 6. 创建项目目录
echo "📂 创建项目目录..."
mkdir -p ~/labor-ai-demo
cd ~/labor-ai-demo

# 7. 安装依赖
echo "📦 安装Python依赖..."
python3 -m venv venv
source venv/bin/activate
pip install langchain==0.1.20 langchain-community==0.0.38 chromadb==0.4.24 streamlit==1.32.2 sentence-transformers==2.6.1

# 8. 配置防火墙
echo "🔥 配置防火墙..."
sudo ufw allow 22
sudo ufw allow 80
sudo ufw allow 8501
echo "y" | sudo ufw enable

echo ""
echo "========================================="
echo "✅ 部署环境准备完成！"
echo "========================================="
echo ""
echo "下一步："
echo "1. 上传项目文件到 ~/labor-ai-demo/"
echo "2. 无需修改代码（默认使用 qwen2.5:3b）"
echo "3. 启动服务"
echo "========================================="
```

---

## 五、上传项目文件到服务器

### 方法1: 使用WinSCP（推荐，Windows用户）

1. 下载WinSCP：https://winscp.net/
2. 新建连接：
   ```
   主机名：你的服务器IP
   端口：22
   用户名：root
   密码：你的密码
   ```
3. 连接后，把本地 `D:\1-myFiles\Desktop\AI实习\labor-ai-demo\` 整个文件夹拖到服务器的 `/root/labor-ai-demo/`

### 方法2: 使用SCP命令（命令行）

在本地电脑运行：
```bash
# 进入项目目录
cd D:\1-myFiles\Desktop\AI实习

# 压缩项目
tar -czf labor-ai-demo.tar.gz labor-ai-demo/

# 上传（替换成你的IP）
scp labor-ai-demo.tar.gz root@你的服务器IP:~/

# SSH登录服务器
ssh root@你的服务器IP

# 解压
cd ~
tar -xzf labor-ai-demo.tar.gz
```

---

## 六、模型配置

程序默认使用 `qwen2.5:3b`（小服务器可流畅运行），无需修改代码。

如需用其他模型（机器配置较高时），通过环境变量切换：

```bash
cd ~/labor-ai-demo

# 临时生效（当前终端）
export OLLAMA_MODEL=qwen2.5:7b
streamlit run app.py

# 永久生效
echo 'export OLLAMA_MODEL=qwen2.5:7b' >> ~/.bashrc
source ~/.bashrc
```

---

## 七、启动服务

### 临时启动（测试用）

```bash
cd ~/labor-ai-demo
source venv/bin/activate
streamlit run app.py --server.port 8501 --server.address 0.0.0.0
```

在浏览器访问：`http://你的服务器IP:8501`

### 后台运行（演示用）

```bash
cd ~/labor-ai-demo
source venv/bin/activate
nohup streamlit run app.py --server.port 8501 --server.address 0.0.0.0 > streamlit.log 2>&1 &
```

**查看日志**：
```bash
tail -f ~/labor-ai-demo/streamlit.log
```

**停止服务**：
```bash
ps aux | grep streamlit
kill <进程号>
```

---

## 八、测试访问

### 浏览器测试

打开浏览器，输入：`http://你的服务器IP:8501`

✅ 看到界面说明部署成功！

### 测试对话

在界面中输入：
- "你好，有什么普工岗位吗？"
- "工资多少？"

✅ AI能正常回答说明运行正常！

---

## 九、中秋节演示准备

### 演示链接

准备一个短链接给家里人：
```
http://你的服务器IP:8501

或者绑定域名后：
http://ai.quantonglabor.com
```

### 演示话术

```
"爸妈，您看，我已经把系统部署到云端了。
 您现在就可以打开这个链接试试，
 问问有什么岗位、工资多少，AI会自动回答。"
```

**优势**：
- ✅ 不用带笔记本
- ✅ 家里人可以直接手机访问
- ✅ 显得更专业
- ✅ 24小时在线

---

## 十、监控与维护

### 查看系统资源

```bash
# 安装监控工具
sudo apt install htop

# 查看CPU和内存
htop

# 查看磁盘
df -h
```

### 查看服务状态

```bash
# Ollama状态
sudo systemctl status ollama

# Streamlit日志
tail -f ~/labor-ai-demo/streamlit.log
```

### 重启服务

```bash
# 杀死旧进程
pkill -f streamlit

# 重新启动
cd ~/labor-ai-demo
source venv/bin/activate
nohup streamlit run app.py --server.port 8501 --server.address 0.0.0.0 > streamlit.log 2>&1 &
```

---

## 十一、成本估算

### Demo阶段（现在）
```
服务器: ¥0（试用）
域名: ¥0（暂不需要）
流量: ¥0-10/月

总计: ¥0-10/月 ✅ 几乎免费！
```

### 正式运营（3个月后）
```
服务器: ¥300/月（升级到8核16G）
域名: ¥10/月
流量: ¥50/月
SSL: ¥0（免费证书）

总计: ¥360/月

但能节省: ¥4000/月（1个客服工资）
净收益: ¥3640/月
```

---

## 十二、常见问题

### Q1: 2核4GB跑qwen2.5:3b卡不卡？
```
A: 可以运行，不会卡死。云服务器没有GPU、核数少，推理比本机慢
   （本机6核CPU实测3b全文约16-18秒），部署后请先实测响应时间。
   7b内存不足会卡，所以用3b。
```

### Q2: 多少人同时用会卡？
```
A: 5-10人同时用没问题。
   Demo阶段不会超过这个数。
```

### Q3: 试用期到期怎么办？
```
A: 3个月后如果效果好，公司愿意出钱续费。
   或者迁移回本地运行（成本¥0）。
```

### Q4: IP地址太难记怎么办？
```
A: 可以：
   1. 发个短链接（用短网址生成器）
   2. 或者买个域名（¥50/年）
   3. 或者加到浏览器收藏夹
```

---

## 十三、快速部署时间表

### ✅ 今天晚上（30分钟）
- [ ] 开通阿里云试用实例（2核4GB）
- [ ] 配置安全组（22, 80, 8501端口）
- [ ] SSH登录测试

### ✅ 明天上午（30分钟）
- [ ] 运行一键部署脚本
- [ ] 上传项目文件
- [ ] 确认模型（默认 qwen2.5:3b，无需修改代码）

### ✅ 明天下午（30分钟）
- [ ] 启动服务
- [ ] 公网访问测试
- [ ] 测试10个问题

### ✅ 后天（优化）
- [ ] 性能优化
- [ ] 准备演示链接
- [ ] 打印二维码（可选）

---

## 🎯 立即行动清单

### 第一步：开通试用实例（现在）
1. 登录阿里云
2. 找到ECS试用
3. 选择：2核4GB，Ubuntu 22.04，深圳
4. 配置安全组：22, 80, 8501
5. 记下公网IP

### 第二步：部署环境（今晚）
1. SSH登录
2. 复制粘贴一键部署脚本
3. 等待完成（约10分钟）

### 第三步：上传项目（明天）
1. 用WinSCP上传整个labor-ai-demo文件夹
2. 无需修改模型（默认 qwen2.5:3b）
3. 启动服务

### 第四步：测试演示（明天）
1. 浏览器打开 http://你的IP:8501
2. 测试10个问题
3. 分享链接给室友试用

---

## ✨ 完成后的效果

**你将拥有**：
- ✅ 一个24小时在线的AI客服系统
- ✅ 一个可以分享的演示链接
- ✅ 一个真实部署的项目经验
- ✅ 中秋节演示的"杀手锏"

**家里人的反应**：
- 😮 "哇，真的能用！"
- 👍 "不用带电脑，直接手机就能看"
- 💼 "这个确实能帮我们省事"
- ✅ "数据给你，继续开发！"

---

**现在就去阿里云开通试用实例，30分钟搞定部署！** 🚀

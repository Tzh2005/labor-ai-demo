# 快速启动指南 - 30分钟上手

> **适用场景**：已完成初次部署，需要快速启动Demo

---

## 🚀 快速启动（3步）

### Step 1: 启动Ollama服务（如果未运行）

打开Git Bash，检查服务状态：

```bash
curl http://localhost:11434
```

如果提示连接失败，启动服务：

```bash
ollama serve
```

**保持这个窗口打开**，不要关闭。

---

### Step 2: 进入项目并激活环境

打开**新的** Git Bash窗口：

```bash
cd D:/1-myFiles/Desktop/AI实习/labor-ai-demo
source venv/Scripts/activate
```

看到命令行前面显示 `(venv)` 就成功了。

---

### Step 3: 启动Demo

```bash
# 已成功运行过一次后，建议先启用离线模式（跳过 HuggingFace 联网检查）
export HF_HUB_OFFLINE=1        # PowerShell 用户改用: $env:HF_HUB_OFFLINE = "1"

streamlit run app.py
```

浏览器会自动打开 http://localhost:8501

---

## 🎯 快速测试

输入这5个问题测试：

1. "有什么普工岗位？"
2. "石岩电子厂工资多少？"
3. "包吃包住吗？"
4. "需要什么证件？"
5. "工资怎么算？"

---

## 🛑 停止服务

在运行 Streamlit 的终端按：`Ctrl + C`

---

## ⚡ 超快速启动（一键脚本）

**双击运行**：
```
一键部署脚本.bat
```

脚本会自动：
- 检查Ollama服务
- 激活虚拟环境
- 启动Streamlit

---

## 🐛 常见问题

### 1. Ollama连接失败

```bash
# 检查服务
curl http://localhost:11434

# 如果失败，启动服务
ollama serve
```

### 2. 虚拟环境激活失败

```bash
# 重新创建虚拟环境
cd D:/1-myFiles/Desktop/AI实习/labor-ai-demo
rm -rf venv
python -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 3. 端口被占用

```bash
# 使用其他端口
streamlit run app.py --server.port 8502
```

### 4. 向量库损坏

```bash
# 删除重建
rm -rf chroma_db/
streamlit run app.py
```

### 5. 提问后卡住不动好几分钟

```bash
# 原因：嵌入模型联网检查 HuggingFace 超时（国内网络常见）
# 程序已内置离线优先加载；如仍卡顿，启动前设置：
export HF_HUB_OFFLINE=1        # PowerShell 用户改用: $env:HF_HUB_OFFLINE = "1"
```

### 6. 提问时卡住或报 502（代理软件导致）

```bash
# 原因：开启系统代理（Clash / v2ray 等）时，httpx 库不会读取 Windows
# 的代理例外名单（ProxyOverride），把 localhost 的 Ollama 请求也发给代理
# 程序已内置 NO_PROXY 修复；如仍异常，启动前设置：
export NO_PROXY=localhost,127.0.0.1   # PowerShell: $env:NO_PROXY = "localhost,127.0.0.1"
# 或临时关闭系统代理（设置 → 网络和 Internet → 代理）
```

---

## 📝 完整命令速查

### Windows (Git Bash)

```bash
# 1. 启动Ollama（新窗口，保持运行）
ollama serve

# 2. 启动Demo（新窗口）
cd D:/1-myFiles/Desktop/AI实习/labor-ai-demo
source venv/Scripts/activate
export HF_HUB_OFFLINE=1        # 跳过 HuggingFace 联网检查（PowerShell: $env:HF_HUB_OFFLINE = "1"）
streamlit run app.py

# 3. 测试模型（可选）
ollama run qwen2.5:3b "你好"

# 4. 查看已安装模型
ollama list

# 5. 重建向量库
rm -rf chroma_db/
```

### 使用一键脚本

```bash
# 进入目录
cd D:/1-myFiles/Desktop/AI实习/labor-ai-demo

# 双击运行或命令行执行
./一键部署脚本.bat
```

---

## 🎬 演示准备

### 提前准备（5分钟）

1. **启动服务**
   ```bash
   ollama serve
   ```

2. **启动Demo**
   ```bash
   cd labor-ai-demo
   source venv/Scripts/activate
   streamlit run app.py
   ```

3. **测试一遍**
   - 输入5个测试问题
   - 确认回答正常
   - 速度在5秒内

4. **准备演示词**
   - 查看 `中秋节演示准备清单.md`
   - 背熟5分钟脚本

### 演示方案

**方案A：本地演示**（推荐）
- 打开 http://localhost:8501
- 不需要网络
- 速度快，稳定

**方案B：云端演示**（如果已部署）
- 手机访问 http://你的IP:8501
- 更方便展示
- 需要网络

**方案C：截图备份**
- 提前截图3-5张
- 如果现场出问题，用截图展示

---

## 📊 性能参考

**启动时间**：
- Ollama服务：1-2秒
- Streamlit应用：5-10秒
- 首次构建向量库：10-15秒

**运行性能**（本机 CPU 实测，1655 token 的 RAG 提问）：
- 3b：首字约 10-15 秒，全文约 16-18 秒
- 7b：首字约 45 秒，全文约 52 秒
- 内存占用：3b 约 2.5GB，7b 约 5GB

**系统要求**：
- 最低：4核8GB（使用 qwen2.5:3b）
- 推荐：6核16GB（使用 qwen2.5:7b）
- 注意：Ollama 常驻内存较大，开演示前关闭无关程序可避免 502 等内存不足问题

---

## 🔄 日常使用流程

### 每次启动

```bash
# 终端1：启动Ollama
ollama serve

# 终端2：启动Demo
cd labor-ai-demo
source venv/Scripts/activate
export HF_HUB_OFFLINE=1        # 跳过 HuggingFace 联网检查（PowerShell: $env:HF_HUB_OFFLINE = "1"）
streamlit run app.py
```

### 每次关闭

```bash
# 在Streamlit终端按 Ctrl+C
# 关闭Ollama终端窗口
```

### 更新数据

编辑数据文件后，需要重建向量库：

```bash
# 删除旧向量库
rm -rf chroma_db/

# 重新启动（会自动重建）
streamlit run app.py
```

---

## 📁 项目文件说明

```
labor-ai-demo/
├── app.py                      # 主界面（Streamlit）
├── ai_service.py               # AI服务（LangChain + Ollama）
├── knowledge_base.py           # 知识库（ChromaDB）
├── requirements.txt            # Python依赖
├── 一键部署脚本.bat            # 自动部署脚本
├── 本地部署手册.md             # 详细部署教程
├── 中秋节演示准备清单.md       # 演示准备
├── 快速启动.md                 # 本文件
├── venv/                       # 虚拟环境（自动生成）
├── chroma_db/                  # 向量数据库（自动生成）
└── data/
    ├── jobs.json               # 岗位数据
    └── faq.md                  # 常见问题
```

---

## 💡 使用技巧

### 更换模型（提速 / 提质）

程序默认使用 `qwen2.5:3b`（本地 CPU 速度最快）。实测本机 CPU：3b 全文约 16-18 秒，7b 全文约 52 秒。

```bash
# 需要更好回答质量时（机器性能较强，如上云后）：
ollama pull qwen2.5:7b

# 临时使用（当前窗口有效）
export OLLAMA_MODEL=qwen2.5:7b          # PowerShell: $env:OLLAMA_MODEL = "qwen2.5:7b"
streamlit run app.py

# 永久固定（重开终端生效）
setx OLLAMA_MODEL qwen2.5:7b
```

确认不再使用 7b 后，可运行 `ollama rm qwen2.5:7b` 释放约 4.7GB 磁盘（随时可重新 pull 恢复）。

### 修改检索数量

编辑 `ai_service.py`，第35行：

```python
results = self.kb.search(question, k=3)  # k=检索数量，默认3
```

### 添加岗位数据

编辑 `data/jobs.json`，按格式添加：

```json
{
  "id": 21,
  "factory_name": "新工厂名称",
  "position": "新岗位",
  "salary": "5000-8000",
  ...
}
```

保存后，删除 `chroma_db/`，重新启动。

### 添加FAQ

编辑 `data/faq.md`，按格式添加：

```markdown
## 问题标题

**问题**：工人常问的问题？

**回答**：标准答案...
```

---

## 🎯 下一步

- 📄 查看 `本地部署手册.md` - 首次部署指南
- 📄 查看 `阿里云部署指南.md` - 云端部署教程
- 📄 查看 `中秋节演示准备清单.md` - 演示准备

---

**需要帮助？回到项目目录查看完整文档！** 📚

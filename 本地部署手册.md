# 劳务派遣AI客服Demo - 本地部署手册

> **目标**：30分钟内完成本地部署并测试
> **适用系统**：Windows 11

---

## 📋 部署前检查

### 系统要求
- ✅ Windows 11
- ✅ 至少8GB内存（推荐16GB）
- ✅ 10GB以上可用磁盘空间
- ✅ 稳定的网络连接

### 需要安装的软件
- [ ] Python 3.10+
- [ ] Ollama
- [ ] Git Bash（已有）

---

## 🚀 Step 1: 安装Ollama（10分钟）

### 1.1 下载Ollama

访问官网下载：
```
https://ollama.com/download/windows
```

或者直接下载：
```
https://ollama.com/download/OllamaSetup.exe
```

### 1.2 安装Ollama

1. 双击 `OllamaSetup.exe`
2. 按照提示完成安装（默认安装到 `C:\Users\你的用户名\AppData\Local\Programs\Ollama`）
3. 安装完成后，Ollama会自动启动并在后台运行

### 1.3 验证安装

打开**新的** Git Bash终端（重要：需要重新打开）：

```bash
ollama --version
```

**预期输出**：
```
ollama version is 0.x.x
```

如果显示 `command not found`，请：
1. 关闭所有Git Bash窗口
2. 重新打开Git Bash
3. 再次运行命令

### 1.4 启动Ollama服务

```bash
# 检查Ollama是否在运行
curl http://localhost:11434

# 预期输出: "Ollama is running"
```

如果没有运行，手动启动：
```bash
ollama serve
```

**重要**：保持这个终端窗口打开，不要关闭！

---

## 📦 Step 2: 下载AI模型（15分钟）

### 2.1 下载默认模型 qwen2.5:3b

**打开新的Git Bash终端**（保留ollama serve那个窗口），运行：

```bash
ollama pull qwen2.5:3b
```

**下载时间**：约5-10分钟（取决于网速）
**模型大小**：约1.9GB

> 机器配置高、想要更好回答质量时：`ollama pull qwen2.5:7b`（约4.7GB），
> 并设置 `OLLAMA_MODEL=qwen2.5:7b`（无需修改代码）。

### 2.2 测试模型

下载完成后，测试一下：

```bash
ollama run qwen2.5:3b "你好，请用一句话介绍你自己"
```

**预期输出**：模型会用中文回复

退出测试：
```bash
/bye
```

### 2.3 备选：切换模型

3b（默认）速度快、占用内存小；机器配置较高时可用 7b 获得更好回答质量：

```bash
ollama pull qwen2.5:7b
```

切换只需设置环境变量 `OLLAMA_MODEL`，无需修改代码。

---

## 🐍 Step 3: 安装Python依赖（5分钟）

### 3.1 检查Python版本

```bash
python --version
```

**要求**：Python 3.10 或更高

如果没有Python，下载安装：
```
https://www.python.org/downloads/
```

### 3.2 进入项目目录

```bash
cd D:/1-myFiles/Desktop/AI实习/labor-ai-demo
```

### 3.3 创建虚拟环境（推荐）

```bash
python -m venv venv
```

### 3.4 激活虚拟环境

```bash
source venv/Scripts/activate
```

激活成功后，命令行前面会显示 `(venv)`

### 3.5 安装依赖包

```bash
pip install -r requirements.txt
```

**安装时间**：约2-5分钟

**如果安装慢**，使用国内镜像：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 3.6 验证安装

```bash
pip list | grep -E "streamlit|langchain|chromadb"
```

**预期输出**：应该看到这些包已安装

---

## 🎯 Step 4: 启动Demo（2分钟）

### 4.1 启动Streamlit应用

确保：
- ✅ Ollama服务在运行（`ollama serve`）
- ✅ 虚拟环境已激活（看到 `(venv)`）
- ✅ 在项目目录下

运行：
```bash
streamlit run app.py
```

**首次启动**：
- 需要下载嵌入模型（bge-large-zh-v1.5），约30秒
- 需要构建向量数据库，约10秒

### 4.2 打开浏览器

Streamlit会自动打开浏览器，访问：
```
http://localhost:8501
```

如果没有自动打开，手动访问上面的地址。

---

## ✅ Step 5: 测试功能（5分钟）

### 5.1 基础问答测试

在聊天框中输入以下问题：

**测试1：岗位查询**
```
有什么普工岗位？
```

**测试2：具体工厂**
```
石岩电子厂工资多少？
```

**测试3：福利待遇**
```
包吃包住吗？
```

**测试4：入职流程**
```
需要什么证件？
```

**测试5：工资计算**
```
工资怎么算？
```

### 5.2 多轮对话测试

测试AI的记忆功能：

```
问题1: 有女生适合的工作吗？
问题2: 这个工厂包住吗？（AI应该记得你在问哪个工厂）
问题3: 工作时间呢？（继续追问同一个工厂）
```

### 5.3 验收标准

- [ ] Demo能正常启动
- [ ] 界面加载正常，没有报错
- [ ] 能回答岗位相关问题（准确率80%+）
- [ ] 响应速度<5秒
- [ ] 多轮对话有记忆
- [ ] 界面清晰易用

---

## 🐛 常见问题排查

### 问题1：Ollama连接失败

**错误信息**：
```
ConnectionRefusedError: [Errno 111] Connection refused
```

**解决方案**：
1. 检查Ollama是否在运行：
   ```bash
   curl http://localhost:11434
   ```
2. 如果没有运行，启动它：
   ```bash
   ollama serve
   ```

### 问题2：模型下载慢或失败

**解决方案1**：使用国内镜像（如果有）

**解决方案2**：改用默认小模型（下载更快、响应更快）
```bash
ollama pull qwen2.5:3b
```

程序默认就使用 qwen2.5:3b（无需修改代码）；如之前设置过其他模型，启动前运行：
```powershell
$env:OLLAMA_MODEL = "qwen2.5:3b"
```

### 问题3：端口8501被占用

**错误信息**：
```
Port 8501 is already in use
```

**解决方案**：换个端口
```bash
streamlit run app.py --server.port 8502
```

### 问题4：向量数据库报错

**错误信息**：
```
chromadb.errors.InvalidCollectionException
```

**解决方案**：删除并重建
```bash
rm -rf chroma_db/
streamlit run app.py
```

### 问题5：内存不足

**错误信息**：
```
OOM (Out of Memory)
```

**解决方案**：
1. 关闭其他占内存的程序
2. 确认使用小模型（默认 qwen2.5:3b）
3. 减少检索数量（修改 `ai_service.py` 中的 `k=3` 改为 `k=2`）

### 问题6：Python依赖安装失败

**解决方案**：
```bash
# 升级pip
python -m pip install --upgrade pip

# 使用清华源重新安装
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 📸 截图保存

如果Demo运行成功，记得截图保存：

1. **主界面截图**：显示聊天界面
2. **问答截图**：展示几个问答效果
3. **多轮对话截图**：展示记忆功能

**用途**：明天演示用，以防现场网络问题。

---

## 🎯 完成标志

当你完成以下步骤，就可以进行下一步（云端部署）了：

- [x] Ollama已安装并运行
- [x] AI模型已下载（默认 qwen2.5:3b）
- [x] Python依赖已安装
- [x] Demo能正常启动
- [x] 测试5个问题都正常回答
- [x] 多轮对话有记忆
- [x] 已截图保存

---

## 📝 下一步

完成本地部署后，查看：
- 📄 `阿里云部署指南.md` - 云端部署教程
- 📄 `快速启动.md` - 快速参考手册

---

**预计总时间**：30-40分钟
**当前进度**：Step 1 - 等待执行

🎉 **加油！完成本地部署是第一步！**
